<h1>修改用户信息</h1>

<%= render 'edit_form' %>

<%= link_to '取消修改信息', user_index_path %> |
<% if User.find_by_id(session[:user_id]).admin? %>
<%= link_to '返回用户列表', users_path %>
<% else %>
<%= link_to '返回网站主页', root_path %>
<% end %>
