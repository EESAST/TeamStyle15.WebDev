<div id="right">
  <ul>
    <h3>导航</h3>
<%if current_user.id==@team.captain_id||current_user.admin?-%>
    <li><%= link_to '修改队伍名称', edit_team_path(@team) %></li>
    <li><%= link_to '解散队伍', team_path(@team), method: :delete, data: { confirm: '此操作不可恢复。确定？' } %></li>
<%end-%>
<%if current_user.team_id==nil-%>
    <li><%= link_to '加入一支队伍',teams_path-%></li>
<%end-%>
    <li><%= link_to '用户主页', user_index_path %></li>
    <li><%= link_to '队伍列表', teams_path %></li>
  </ul>
</div>
<div id="content">
  <h2>队伍详情<br/><br/></h2>
  <table style="width:500px;margin:auto">
    <thead>
      <th colspan=2>队伍信息</th>
    </thead>
    <tr>
      <td id="left-item"><strong>队伍名称:</strong></td>
      <td id="right-item"><%= @team.name %></td>
    </tr>
    <tr>
      <td id="left-item"><strong>队长:</strong></td>
      <td id="right-item"><%= (@team.captain_id)?(link_to (@captain=User.find(@team.captain_id)).name,@captain):link_to("成为队长","/teams/#{@team.id}/add/#{current_user.id}")%></td>
    </tr>
    <tr>
      <td id="left-item"><strong>队员:</strong></td>
      <td id="right-item">
<%@team.users.each do |user| -%>
      <%=link_to user.name,user%>&emsp;<%=link_to '踢出队伍',"/teams/#{@team.id}/kick/#{user.id}",method: :delete,data: {confirm:"确定要把#{user.name}踢出队伍？此操作不可恢复。"} if (current_user.id==@team.captain_id||current_user.admin?)&&@team.captain_id!=user.id-%></br>
<%end-%>
      </td>
    </tr>
    <tr>
      <td id="left-item"><strong>成立时间:</strong></td>
      <td id="right-item"><%=@team.created_at.in_time_zone('Beijing').strftime('%Y年%m月%d日%H时')%></td>
    </tr>
  </table>
<%if current_user.id==@team.captain_id%>
    <h2>队伍管理<br/><br/></h2>
<%emd-%>
</div>
