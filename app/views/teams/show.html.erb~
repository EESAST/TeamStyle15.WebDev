<p id="notice"><%= notice %></p>

<p>
  <strong>队伍名称:</strong>
  <%= @team.name %>
</p>

<p>
  <strong>队长:</strong>
  <%= User.find(@team.captain_id).name %>
</p>

<%if !@team.users.empty? -%>
<p>
  <strong>队员:</strong>
<%@team.users.each do |user| -%>
  <%=user.name%>
<%end-%>
</p>
<%end-%>

<%if current_user.id==@team.captain_id||current_user.admin?-%>
  <%= link_to '修改队伍名称', edit_team_path(@team) %> |
  <%= link_to '解散队伍', team_path(@team), method: :delete, data: { confirm: '此操作不可恢复。确定？' } %> |
<%end-%>
<%if current_user.team_id==nil-%>
  <%= link_to '加入一支队伍',teams_path-%> |
<%end-%>

<%= link_to '返回用户主页', user_index_path %> |
<%= link_to '返回网站主页', root_path %>
<%if User.find(session[:user_id]).admin?-%>
 |<%= link_to '返回队伍列表', teams_path %>
<%end-%>
