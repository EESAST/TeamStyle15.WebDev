<% current_user = User.find_by_id(session[:user_id]) %>
<h1>用户<%= current_user.name %></h1>
<h3>欢迎使用</h3>
<p id="notice"><%= notice %></p>

<p>
  <strong>用户名:</strong>
  <%= current_user.name %>
</p>
<div>
  <%= image_tag current_user.gravatar_url size: 100 %>
</div>

<p>
  <strong>电子邮箱:</strong>
  <%= current_user.email %>
</p>

<p>
  <strong>姓名:</strong>
  <%= current_user.true_name %>
</p>

<p>
  <strong>学号:</strong>
  <%= current_user.student_number %>
</p>

<p>
  <strong>所在队伍:</strong>
  <%if current_user.team_id-%>
    <%= Team.find(current_user.team_id).name %>
  <%else-%>尚未加入任何队伍<%end-%>
</p>

<p>
  <strong>用户类型:</strong>
  <% if current_user.admin? %>
    <%= "管理员"%>
  <% else %>
    <%= "参赛选手" %>
  <% end %>
</p>

<%= link_to '修改用户信息', edit_user_path(current_user) %> 
<%= link_to '查看队伍列表', teams_path %> |
<%if current_user.team_id-%>
  <%if Team.find(current_user.team_id).captain_id==current_user.id-%>
    <%= link_to '管理所在队伍', team_path(current_user.team_id) %> |
  <%else-%>
    <%= link_to '查看所在队伍', team_path(current_user.team_id) %> |
  <%end-%>
  <%= link_to '退出所在队伍', "/teams/#{current_user.team_id}/del",method: :delete %> |
<%else-%>
  <%= link_to '创建一支队伍', new_team_path %> |
  <%if Team.count!=0%>
    <%= link_to '加入一支队伍', teams_path %> |
  <%end-%>
<%end-%>
<%= link_to '返回网站主页', root_path %>


